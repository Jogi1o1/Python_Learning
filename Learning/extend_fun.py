# extend([item1,item2,...]) function is used add a list of items into a list.
# declaring a list here
List_features = ["Portfolio", "Loan_upload", "Payments"]
# Now we are adding a list of items at the end of this existing list
List_features.extend(["User", "Filter", "Export"])
# Now we are printing the list
print(List_features)
